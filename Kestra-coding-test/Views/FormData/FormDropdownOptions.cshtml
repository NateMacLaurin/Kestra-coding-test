@model IEnumerable<Kestra_coding_test.Models.FormDropdownOptions>

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">FormDropdownOptions - Data</h2>
        </div>
        <div class="col-6 align-content-end">
            <a class="nav-link" asp-controller="FormData" asp-action="Index">Back to Form Data</a>
        </div>
    </div>
        
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width: 100%">
            <thead>
                <tr>
                    <th>
                        SelectorValue
                    </th>
                    <th>
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="50%">@obj.SelectorValue</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No FormDropdownOptions Exist!</p>
    }
</div>